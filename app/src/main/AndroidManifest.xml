<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.foi.air.in4maticsquiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OdabirRazredaActivity"
            android:label="@string/title_activity_odabir_razreda"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistracijaActivity"
            android:label="@string/title_activity_registracija"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_registracija"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProvjeriZnanjeActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" />
        <activity android:name=".RangListeActivity" />
        <activity
            android:name=".PoglavljaActivity"
            android:label="Ažuriranje poglavlja" />
        <activity
            android:name=".PitanjaActivity"
            android:label="@string/title_activity_pitanja"
            android:theme="@style/AppTheme" />
        <activity android:name=".DodajNovoPitanje"></activity>
    </application>

</manifest>
